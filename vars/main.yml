---
ansible_managed: |
  #############################################################################
  # Ansible managed: Do NOT edit this file manually!                          #
  # Modified on {{ ansible_date_time.date }} at {{ ansible_date_time.time }}                                        #
  #############################################################################

myansible_prefix_task_name: "{{ ansible_distribution | lower }} | {{ ansible_distribution_release }} - "
siege_build_path: "{{ ansible_user_dir }}/tmp"

siege_download_path: "{{ ansible_user_dir }}/downloads"
siege_download_url: "http://download.joedog.org/siege/siege-latest.tar.gz"
